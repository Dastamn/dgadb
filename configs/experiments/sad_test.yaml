model: SAD
# anomaly_as_0: True

anomalies:
  anom_train_ratio: 0.5
  anom_val_ratio: 0.1
  anom_test_ratio: 0.1

hyperparameters:
  epochs: 10
  batch_size: 256



  input_dim:
  # the feature dim of the dataset, it crates 
    default:
      fixed: 1
    mooc:
      fixed: 4