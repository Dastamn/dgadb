# snapshot_size: 1000
# train_ratio: 0.70
# anomaly_ratio: 0.05

dataset:
  name: email-dnc
  paths:
    raw: null
    structured: data/email-dnc
    processed: null

pipeline:
  cache_dir: pipeline_cache/email-dnc
  steps:
    - name: DataLoader
      params:
        dataset_dir: /mnt/disk2/nazim/dgadb/data/email-dnc
    - name: StructureNormalizer
      params:
        directionality: canonical
        reindex_nodes: True
        remove_self_loops: False
        remove_duplicates: False
        
    - name: TimestampNormalizer

    - name: TemporalSplitter
      params:
        train_ratio: 0.7
        val_ratio: 0.1

