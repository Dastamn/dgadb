# snapshot_size: 1000
# train_ratio: 0.70
# anomaly_ratio: 0.05

dataset:
  name: bitcoin-otc
  paths:
    raw: null
    structured: data/bitcoin-otc
    processed: null

pipeline:
  cache_dir: pipeline_cache/bitcoin-otc
  steps:
    - name: DataLoader
      params:
        dataset_dir: /mnt/disk2/nazim/dgadb/data/bitcoin-otc
    - name: StructureNormalizer
      params:
        directionality: canonical
        reindex_nodes: True
        remove_self_loops: False
        remove_duplicates: False
        
    - name: TimestampNormalizer

    - name: TemporalSplitter
      params:
        train_ratio: 0.7
        val_ratio: 0.1

